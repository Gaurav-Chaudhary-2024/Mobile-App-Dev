<?xml version="1.0" encoding="utf-8"?>
<!--
    Single image item in the gallery grid.
    Square shape — width = 1/3 of screen.
    MaterialCardView gives rounded corners + elevation.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:layout_margin="4dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp">

    <!--
        ImageView fills the entire card.
        scaleType="centerCrop" crops image to fill
        the square without distortion — like Instagram grid.
    -->
    <ImageView
        android:id="@+id/img_thumbnail"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="Image thumbnail" />

</com.google.android.material.card.MaterialCardView>